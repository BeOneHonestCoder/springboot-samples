filebeat.inputs:
- type: log
  enabled: true
  paths:
    - /var/log/app-logs/*.log
  # Handle multi-line logs (like Java Stack Traces) if needed
  multiline.pattern: '^[0-9]{4}-[0-9]{2}-[0-9]{2}'
  multiline.negate: true
  multiline.match: after

output.logstash:
  hosts: ["logstash:5044"]